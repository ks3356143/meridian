# ChenMeridian 版本记录

## V0.0.2 - 2026-09-13

- 新增预挂载品牌首屏：内联经线球、渐变字标与流光，消除前端冷启动白屏；`index.html` 预置暗色主题，消除主题闪白。
- 路由加载态升级为 React Bits `ShinyText` 品牌加载页，引入 `motion` 依赖。
- `sonner` 顶部提示新增安全偏移（`--toast-safe-top`），完整避让固定顶栏，桌面与移动端同步生效。
- 登录页流程轨节点改为等距分布，信号推进时机与节点位置对齐。
- 工作区顶栏视觉分层：安全等级徽标改为实心语义色（A 红 / B 橙 / C 蓝），全局轮次切换器改为明确的下拉按钮。
- 模块占位壳翻新：图标与标题一行化、删除"二级导航"元信息徽标、壳高度跟随内容、新增模块色径向光晕与激活项色轨；执行总览 KPI 数字对齐 tabular-nums 规范。
- 整理需求文档与项目记忆，补录白屏成因、`currentColor` 渐变陷阱等可复用坑位。

## V0.0.1 - 2026-09-08

- 初始化 Go + Huma + GORM + pure Go SQLite 后端骨架。
- 增加健康检查接口 `/api/v1/health`，接口会实际查询 SQLite 版本。
- 启用 Huma 自动生成的 `/docs` API 文档和 `/openapi.json`。
- 建立程序、接口、配置、数据库、迁移、前端、数据、附件、报告、模板目录。
