# 跨会话记忆

本文件是跨会话记忆的固定入口。每次会话开工时先读本文件，再按需读取全局记忆，避免遗漏上次会话留下的任务与偏好。

## 全局记忆位置

以下路径位于用户主目录，不随仓库提交：

- 全局规则：`C:\Users\31429\.codex\AGENTS.md`（每次会话自动加载）。
- 记忆手册：`C:\Users\31429\.codex\memories\MEMORY.md`。
- 记忆摘要：`C:\Users\31429\.codex\memories\memory_summary.md`。
- 任务备忘：`C:\Users\31429\.codex\memories\extensions\ad_hoc\notes\`，按日期命名。

## 使用规则

1. 每次会话开工，先读本文件，再读取任务备忘目录中与当前工作相关的文件。
2. 查找跨会话内容时，优先用 `rg` 在 `C:\Users\31429\.codex\memories` 全目录做关键词搜索，不要只看根目录。
3. 会话结束时，如有未完成任务，把任务摘要写入任务备忘目录，并在本文件追加一行指针；任务完成后删除对应指针。

## 2026-09-14 会话进度快照

本日提交：`26f60a0 重构大工作区并接入工作对象生命周期`，已推送 `origin/main`。

### 已完成

1. 大工作区第二轮架构落地：
   - 一级导航收敛为作业台、资料与对象、需求与追踪、测试与执行、交付与文档。
   - 首屏改为当前任务 + 数据主链 + 测试项组织树示意。
   - 口径改为项目 -> 工作对象 -> 软件需求 -> 测试项 -> 测试用例 -> 问题单。
   - 系统 / 分系统 / 配置项不再是权威实体层级，只作为测试项组织树中的文件夹语义。
2. 接收文件真实录入：
   - 新增 `work_objects`、`work_object_versions`、`received_assets`。
   - 文件保存到 `data/file-assets/projects/{项目}/{工作对象}/{版本}/`。
   - 支持批量上传、手工登记、行内修改、确认、删除、刷新回显。
   - 上传时识别对象类型、版本、平台和解析状态。
   - 新增“用户手册”对象类型；迁移扩展为 `0009_work_object_user_manual`。
   - 上传进度读取真实字节，处理中阻断操作，完成后保留结果并由用户手动关闭。
3. 工作对象版本生命周期：
   - 状态为 `draft / confirmed / superseded / revoked`。
   - 确认新版本自动替代旧版本。
   - 撤回确认会恢复最近被替代版本。
   - 作废保留记录、文件和审计原因。
   - 新增生命周期事件表和前端历史记录弹窗。
4. 路由与体验：
   - 未登录深链显示统一 401 状态页，可返回登录并回跳原地址。
   - 已登录未知路由显示统一 404 状态页。
   - 上传页布局改为窄左侧投放 / 接收信息 + 宽右侧队列。
   - 表格表头和短列内容居中，长文本列显式左对齐。
5. 控制台问题：
   - 修复 `next-themes` 在 React 19 开发环境渲染 script 的警告。
   - 对所有直接 `.toLowerCase()` 调用增加空值保护。
6. 稳定性与性能：
   - 根因修复 Edge/Chromium 静态文字选中后的 drag-copy 假死，输入框与文件拖入不受影响。
   - 修复批量上传中途失败不清理落盘文件、对象名大小写不一致、同版本冲突提示和完成态列表刷新仍转圈的问题。
   - 接收文件表格改为行级 memo + 稳定回调 + 行级渲染输出比较；80 行输入从 14 个长任务降到 0。
   - 新增《21-前端性能优化策略》，固化性能定位、修复模式和验收门禁。

### 当前状态

- `R2657` 中已有用户正式上传的 SRS V1.01 和用户手册 V1.00 工作对象，测试数据未污染该项目。
- 前端 `5173`、后端 `8787` 均可运行。
- `knowledge-base/接收文件/` 仍是用户提供的未跟踪资料，禁止提交或删除。
- 后端迁移已应用到本地正式库：`0007_work_objects`、`0008_work_object_lifecycle`、`0009_work_object_user_manual`。

### 下次优先级

1. 需求解析确认页：只允许当前 `confirmed` 的 SRS / 顶层依据版本进入解析，生成候选软件需求。
2. 需求字段确认：章节号、名称、描述、需求类型、平台、来源工作对象版本。
3. 后续再做测试项组织树真实交互和需求 -> 测试项追踪。

## 历史任务指针

- `C:\Users\31429\.codex\memories\extensions\ad_hoc\notes\20260914-chenmeridian-workspace-assets.md`：2026-09-14 大工作区、接收文件、工作对象生命周期与下次上传加载态任务的全局进度备忘。
- `20260911-chenmeridian-frontend-polish.md`：创建项目流程测试与若干视觉确认仍待有机会时回归。
- `20260912-chenmeridian-workspace-blueprint.md`：旧大纲编制流已被第二轮架构吸收，真实内容块和轮次 API 仍待接入。
- `20260913-chenmeridian-workspace-layer-discussion.md`：分层讨论结论已落入《20-大工作区架构落地》，该讨论任务关闭。

## 长期偏好

- 大前端内容设计优先使用 taste-skill 确定审美与体验方向；需要华丽、炫技的交互或视觉时优先使用 React Bits。
- 所有界面美观需求和用户视觉反馈必须先录入《07-前端体验》或对应模块 SRS，再进入实现；实现后同步回写需求文档。
